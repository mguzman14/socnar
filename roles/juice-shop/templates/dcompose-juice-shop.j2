services:
  juice-shop:
    image: bkimminich/juice-shop
    container_name: juice-shop
    restart: always
    ports:
      - "3001:3000"
    networks:
      - network_all
    labels:
      - "traefik.enable=true"      

networks:
  network_all:
    external: true