---
- name: "Crear directorios de configuración para el Perímetro"
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: '0755'
  loop:
    - /opt/traefik
    - /opt/traefik/rules
    - /opt/coraza/config
    - /opt/socnar/edge  # <-- Directorio nuevo para el docker-compose.yml
  become: true

- name: "Garantizar que existe la red network_all"
  community.docker.docker_network:
    name: network_all
    state: present
  become: true

- name: "Instalar y configurar Fail2Ban (Protección SSH)"
  block:
    - name: "Instalar Fail2Ban"
      tags: fail2ban
      ansible.builtin.apt:
        name: fail2ban
        state: present

    - name: "Configurar jail local para SSH"
      tags: fail2ban
      ansible.builtin.copy:
        dest: /etc/fail2ban/jail.local
        content: |
          [sshd]
          enabled = true
          port = ssh
          filter = sshd
          logpath = /var/log/auth.log
          maxretry = 3
          bantime = 1h
      notify: "Reiniciar Fail2ban"
  become: true

# --- AQUÍ EMPIEZA EL CAMBIO A V2 ---

- name: "Generar archivo docker-compose.yml desde Template"
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: /opt/socnar/edge/docker-compose.yml
    mode: '0644'
  become: true

- name: "Desplegar Traefik + Coraza WAF + CrowdSec (V2)"
  tags: [traefik, coraza, crowdsec]
  community.docker.docker_compose_v2:
    project_src: "/opt/socnar/edge"
    project_name: edge_node
    state: present
  become: true

# --- AQUÍ SIGUEN TUS TAREAS DE CONFIGURACIÓN ---

- name: "Configurar Reglas del WAF (OWASP Core Rule Set)"
  tags: coraza
  ansible.builtin.template:
    src: coraza.conf.j2
    dest: /opt/coraza/config/default.conf
  become: true