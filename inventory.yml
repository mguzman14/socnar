all:
  hosts:
    debian-whale:
      ansible_host: debian-whale.tailbbc703.ts.net
      ansible_user: whale
      server_tailscale_ip: "debian-whale.tailbbc703.ts.net"
      # ansible_connection: local
    debian-shark:
      ansible_host: debian-shark.tailbbc703.ts.net
      ansible_user: shark
      server_tailscale_ip: "shark-whale.tailbbc703.ts.net"
      # ansible_connection: local

  children:
    wazuh_manager:
      hosts:
        debian-whale:
    wazuh_agents:
      hosts:
        debian-whale:
        debian-shark:
      vars:
        wazuh_manager_host: "{{ groups['wazuh_manager'][0] }}"
   